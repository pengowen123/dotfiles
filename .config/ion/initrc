# .ionrc

# Aliases
alias nv = 'nvim'
alias k = 'kak'
alias rgdb = 'rust-gdb'
alias xcopy = 'xclip -sel clipboard'
alias l = 'exa --long --group --header --color-scale'

# Path
export PATH = $PATH:$HOME/bin/
export PATH = $PATH:"$HOME/.cargo/bin"
export PATH = $PATH:"$HOME/clones/ion/target/release"
export PATH = $PATH:"$HOME/.ghcup/bin/"
export PATH = $PATH:"$HOME/.local/bin/"
export PATH = $PATH:"$HOME/.gem/ruby/2.7.0/bin"
export PATH = $PATH:"/usr/lib/jvm/java-19-openjdk/bin/"

# Environment variables
export EDITOR = "kak"
export RUST_SRC_PATH = "$HOME/.multirust/toolchains/nightly-x86_64-unknown-linux-gnu/lib/rustlib/src/rust/src"
#export FZF_FILE_COMMAND = "fd --type f --follow"
export RUST_SRC_PATH=$(rustc +nightly --print sysroot)/lib/rustlib/src/rust/library

# vim: ft=ion
